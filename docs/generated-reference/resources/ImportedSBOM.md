# ImportedSBOM

Imported CycloneDX or SPDX software bill of materials.

## Client access

- **Facade:** `client.ImportedSBOM`
- **API path segment:** `sbom-imports`
- **Scope:** `tenant`

## Operations

| Operation | Supported |
|-----------|-----------|
| `list` | yes |
| `get` | yes |
| `create` | yes |
| `update` | yes |
| `delete` | yes |

## User-space access

Customer tenant semantics (distinct from raw OpenAPI and SDK exposure).
Tenant **admin** (`SYSTEM_ROLE_ADMIN`) is the primary writer unless noted.

| Operation | Customer user-space | SDK exposed |
|-----------|---------------------|-------------|
| `list` | yes | yes |
| `get` | yes | yes |
| `create` | yes | yes |
| `update` | yes | yes |
| `delete` | yes | yes |

## Create

- **Mode:** `both`
- **Model import:** `endorlabs.resources.imported_sbom:ImportedSBOM`
- **Payload model:** `CreateImportedSBOMPayload`

### Payload top-level fields

- `context`
- `meta`
- `processing_status`
- `tenant_meta`

### Create convenience kwargs (flat)

**Spec (flat, promoted into `spec`):**

Required:

- `kind`

Optional:

- `cyclone_dx`
- `cyclone_dx_bytes`
- `spdx`
- `spdx_bytes`

Unknown flat kwargs raise `TypeError`. Use `payload=` or nested `spec=` / `meta=` for full control.

### Examples

**Python:**

```python
client.ImportedSBOM.create(kind="...", namespace='tenant.ns')
```

**endorctl:**

```bash
endorctl api create --resource=ImportedSBOM --namespace=<tenant.ns> -f payload.json
```

## Response / read-only spec fields

Present on responses; not accepted as flat create kwargs:

- `identifier`
- `main_component_purl`
- `supplier_name`
- `version`

_Generated by `devtools/codegen/generate_resource_reference_pages.py`. Do not edit by hand._
