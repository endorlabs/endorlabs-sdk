# SBOMExport

Create-only SBOM export for a package version or project.

## Client access

- **Facade:** `client.SBOMExport`
- **API path segment:** `sbom-export`
- **Scope:** `tenant`

## Operations

| Operation | Supported |
|-----------|-----------|
| `list` | no |
| `get` | no |
| `create` | yes |
| `update` | no |
| `delete` | no |

## User-space access

Customer tenant semantics (distinct from raw OpenAPI and SDK exposure).
Tenant **admin** (`SYSTEM_ROLE_ADMIN`) is the primary writer unless noted.

| Operation | Customer user-space | SDK exposed |
|-----------|---------------------|-------------|
| `list` | no | no |
| `get` | no | no |
| `create` | create-only | yes |
| `update` | no | no |
| `delete` | no | no |

## Create

- **Mode:** `both`
- **Model import:** `endorlabs.resources.sbom_export:SBOMExport`
- **Payload model:** `CreateSBOMExportPayload`

### Payload top-level fields

- `meta`
- `tenant_meta`

### Create convenience kwargs (flat)

**Spec (flat, promoted into `spec`):**

Required:

- `component_type`

Optional:

- `export_parameters`
- `format`
- `hide_private_components`
- `include_test_dependencies`
- `kind`

Unknown flat kwargs raise `TypeError`. Use `payload=` or nested `spec=` / `meta=` for full control.

### Examples

**Python:**

```python
client.SBOMExport.create(component_type="...", namespace='tenant.ns')
```

**endorctl:**

```bash
endorctl api create --resource=SBOMExport --namespace=<tenant.ns> -f payload.json
```

## Response / read-only spec fields

Present on responses; not accepted as flat create kwargs:

- `data`

_Generated by `devtools/codegen/generate_resource_reference_pages.py`. Do not edit by hand._
