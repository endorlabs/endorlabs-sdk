---
url: https://docs.endorlabs.com/rest-api/api-query-builder/
title: endorctl API Query Builder | Endor Labs Docs
downloaded: 2026-01-26 10:07:26
---

endorctl API Query Builder | Endor Labs Docs



* Type to search...

# endorctl API Query Builder

Use our API-based query builder to construct endorctl commands based on actual API operations and parameters.

Beta

endorctl API is the most user-friendly way to interact with the Endor Labs REST API. It handles authentication automatically, provides built-in error handling with clear messages, and supports multiple output formats including json, yaml, and table.

## Prerequisites

Ensure that you complete the following prerequisites before running the commands generated by the API query builder:

* Install the latest version of [endorctl](../../endorctl/install-and-configure/) before running the commands generated by the API query builder.
* Run `endorctl init` to authenticate with Endor Labs. See [endorctl init](../../endorctl/commands/init) for more information.

## About the API query builder

The API query builder allows you to construct [endorctl API](../../endorctl/commands/api/) commands based on API operations and parameters. The tool currently supports all API operations available in [Top REST API Reference](https://docs.endorlabs.com/top-api/). You can refer to endorctl API command documentation to construct your own queries that are not supported by the API query builder. You can also refer to [Use Cases](../using-the-rest-api/use-cases/) and [Advanced Use Cases](../using-the-rest-api/advanced-use-cases/) to get examples of how you can use endorctl API commands.

The tool includes options for timeouts, date filtering, and pagination. It also simplifies complex queries with easy filtering, sorting, and field selection. endorctl also supports grouping queries, which is not currently supported by the API query builder. See [Grouping](../using-the-rest-api/grouping/) for more information.

See the [Top REST API Reference](https://docs.endorlabs.com/top-api/) to get the details of request json for the operations that need a json request body. The request body appears in the API query builder for update operations, but you might need to modify it to fit your needs.

### Endor Labs API Query Builder

Build endorctl API commands. Select a service, operation, and configure parameters.

Clear

Service

Loading services...

Operation

Select a service first...

Namespace

Resource Identifier

**UUID or name of the resource:**  
• **UUID:** The unique identifier of the resource you want to access  
• **Name:** The name of the resource (for example, project name)  
• **Example:** `my-project` or `123e4567-e89b-12d3-a456-426614174000`

Output Format

Default (JSON)
JSON
YAML
Table

Choose the output format for the response

List Parameters

Filters

 Add Filter

**Filter which records to find:** Specify criteria to match records.  
Example: `spec.level == FINDING_LEVEL_CRITICAL` - Find only critical findings  
`meta.name` refers to the **selected resource**'s name field

Select field...

Select sub-field...

equals (==)
not equals (!=)
greater than (>)
greater or equal (>=)
less than (<)
less or equal (<=)
contains
not contains
in
not in
matches (regex)
exists
not exists

Field Mask

**Control which fields to display:** Comma-separated list of fields to return.  
For example, `uuid,meta.name,spec.level`. Shows only UUID, name, and severity level. (for example, "name and level only")  
Use this to reduce response size and focus on relevant data

Page Size

**Control response size:** Number of results per page (max 500).  
Smaller values = faster responses, larger values = fewer API calls  
Cannot be used together with "Count only"

Page Token

Page number to retrieve  
Cannot be used together with "Count only"

Sort Field

Sort Order

Default
Ascending
Descending

Count only

Returns only the total count of matching records, not the actual data  
Cannot be used together with "Page Size" or "Page Token"

Include child namespaces

Timeout

Request timeout (default: 20s). Use format like "30s", "2m", "1h"

From Date

Filter records created after this date

To Date

Filter records created before this date

Update Fields

Select which fields you want to update. Only selected fields will be included in the update\_mask.

**Update Mask:** `No fields selected`

Generate

###### Request Body

JSON to create or update the resource. Modify the JSON to fit your needs.

#### Generated Command

Copy Command

`Select a service and operation to generate a command`

#### Instructions

## Feedback

Was this page helpful?

Yes
No

Thanks for the feedback. Write to us at support@endor.ai to tell us more.

Thanks for the feedback. Write to us at support@endor.ai to tell us more.
