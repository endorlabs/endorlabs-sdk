# Generated by Endor Labs SDK model sync. Do not edit manually.
# Regenerate with: uv run python devtools/model_sync.py
# generated by datamodel-codegen:
#   filename:  <stdin>

from __future__ import annotations

from typing import Any

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field


class PackageLicenseQueryAppliedOverride(BaseModel):
    """
    All overrides that contributed to the final result, ordered root -> target.
    """

    namespace: str | None = None
    """
    Namespace where the contributing override resides.
    """
    override_uuid: str | None = None
    """
    UUID of the contributing PackageLicenseOverride in that namespace.
    """


class FileLocation(BaseModel):
    """
    LicenseFileLocation represents the location of a license, copyright, or notice in source files.
    """

    end_byte: int | None = None
    """
    The end byte of the license/copyright/notice in the file.
    """
    end_line: int | None = None
    """
    The end line of the license/copyright/notice in the file.
    """
    relative_path: str | None = None
    """
    The relative path to the file where the license/copyright/notice was found.
    """
    start_byte: int | None = None
    """
    The start byte of the license/copyright/notice in the file.
    """
    start_line: int | None = None
    """
    The start line of the license/copyright/notice in the file.
    """


class PackageLicenseQueryMergedCopyrightEntry(BaseModel):
    """
    MergedCopyrightEntry represents a single copyright with override and selection state.
    """

    copyright_text: str | None = None
    """
    The copyright text (from override if edited, otherwise from original).
    This is the actual copyright text that will be used.
    """
    file_locations: list[FileLocation] | None = None
    """
    File locations where the copyright was found (from override if edited, otherwise from original).
    """
    identifier: str | None = None
    """
    Stable identifier/key for this copyright (file path from PackageLicense.spec.copyrights map or generated UUID for new copyrights).
    This identifier is used as the key in copyright_overrides and deselected_copyrights maps.
    For copyrights from PackageLicense: uses the file path key from PackageLicense.spec.copyrights map.
    For new copyrights added by user: uses generated UUID prefixed with "new-".
    To determine if a copyright is new: check if identifier starts with "new-" or does not exist in original PackageLicense.
    """
    is_edited: bool | None = None
    """
    Whether this copyright has been edited/overridden.
    true = copyright text has been modified from original, false = using original copyright text.
    Can be derived by checking if identifier exists in copyright_overrides map.
    """
    resolved_namespace: str | None = None
    """
    Origin of the effective value for this entry.
    """
    resolved_override_uuid: str | None = None
    selected: bool | None = None
    """
    Whether this copyright is selected for inclusion in notice file.
    true = selected (will be included), false = deselected (will be excluded).
    Defaults to true if not present in deselected_copyrights map.
    """


class PackageLicenseQueryMergedLicenseEntry(BaseModel):
    """
    MergedLicenseEntry represents a single license with override and selection state.
    """

    file_locations: list[FileLocation] | None = None
    """
    File locations where the license was found (from override if edited, otherwise from original).
    """
    identifier: str | None = None
    """
    Stable identifier for this license (hash calculated from normalized license text).
    This identifier is used as the key in license_overrides and deselected_licenses maps.
    For licenses from PackageLicense: uses PackageLicenseInfo.hash.
    For new licenses added by user: hash is calculated from license_text using SHA256(MakeLicenseKey(normalized_text)).
    To determine if a license is new: check if identifier does not exist in original PackageLicense.
    """
    is_edited: bool | None = None
    """
    Whether this license has been edited/overridden.
    true = license text has been modified from original, false = using original license text.
    Can be derived by checking if identifier exists in license_overrides map.
    """
    license_text: str | None = None
    """
    Effective license text resolved after applying propagation across the namespace chain.
    """
    resolved_namespace: str | None = None
    """
    Origin of the effective value for this entry.
    If no override is applied, resolved_namespace will typically be "oss" and
    resolved_override_uuid will be empty.
    """
    resolved_override_uuid: str | None = None
    selected: bool | None = None
    """
    Whether this license is selected for inclusion in notice file.
    true = selected (will be included), false = deselected (will be excluded).
    Defaults to true if not present in deselected_licenses map.
    """
    source: str | None = None
    """
    Source of the original license (e.g., "code", "package_manager", "declared").
    Helps identify which category the license belongs to.
    """
    spdx_expr: str | None = None
    """
    SPDX expression for the license.
    """


class PackageLicenseQueryMergedNoticeEntry(BaseModel):
    """
    MergedNoticeEntry represents a single notice with override and selection state.
    """

    file_locations: list[FileLocation] | None = None
    """
    File locations where the notice was found (from override if edited, otherwise from original).
    """
    identifier: str | None = None
    """
    Stable identifier for this notice (hash calculated from normalized notice text, or generated UUID for new notices).
    This identifier is used as the key in notice_overrides and deselected_notices maps.
    For notices from PackageLicense: uses hash or composite identifier from original.
    For new notices added by user: hash is calculated from notices_text using SHA256(MakeLicenseKey(normalized_text)).
    To determine if a notice is new: check if identifier does not exist in original PackageLicense.
    """
    is_edited: bool | None = None
    """
    Whether this notice has been edited/overridden.
    true = notice text has been modified from original, false = using original notice text.
    Can be derived by checking if identifier exists in notice_overrides map.
    """
    notices_text: str | None = None
    """
    The notice text (from override if edited, otherwise from original).
    This is the actual notice text that will be used.
    """
    resolved_namespace: str | None = None
    """
    Origin of the effective value for this entry.
    """
    resolved_override_uuid: str | None = None
    selected: bool | None = None
    """
    Whether this notice is selected for inclusion in notice file.
    true = selected (will be included), false = deselected (will be excluded).
    Defaults to true if not present in deselected_notices map.
    """


class GoogleprotobufAny(BaseModel):
    """
    `Any` contains an arbitrary serialized protocol buffer message along with a
    URL that describes the type of the serialized message.

    Protobuf library provides support to pack/unpack Any values in the form
    of utility functions or additional generated methods of the Any type.

    Example 1: Pack and unpack a message in C++.

        Foo foo = ...;
        Any any;
        any.PackFrom(foo);
        ...
        if (any.UnpackTo(&foo)) {
          ...
        }

    Example 2: Pack and unpack a message in Java.

        Foo foo = ...;
        Any any = Any.pack(foo);
        ...
        if (any.is(Foo.class)) {
          foo = any.unpack(Foo.class);
        }
        // or ...
        if (any.isSameTypeAs(Foo.getDefaultInstance())) {
          foo = any.unpack(Foo.getDefaultInstance());
        }

     Example 3: Pack and unpack a message in Python.

        foo = Foo(...)
        any = Any()
        any.Pack(foo)
        ...
        if any.Is(Foo.DESCRIPTOR):
          any.Unpack(foo)
          ...

     Example 4: Pack and unpack a message in Go

         foo := &pb.Foo{...}
         any, err := anypb.New(foo)
         if err != nil {
           ...
         }
         ...
         foo := &pb.Foo{}
         if err := any.UnmarshalTo(foo); err != nil {
           ...
         }

    The pack methods provided by protobuf library will by default use
    'type.googleapis.com/full.type.name' as the type URL and the unpack
    methods only use the fully qualified type name after the last '/'
    in the type URL, for example "foo.bar.com/x/y.z" will yield type
    name "y.z".

    JSON
    ====
    The JSON representation of an `Any` value uses the regular
    representation of the deserialized, embedded message, with an
    additional field `@type` which contains the type URL. Example:

        package google.profile;
        message Person {
          string first_name = 1;
          string last_name = 2;
        }

        {
          "@type": "type.googleapis.com/google.profile.Person",
          "firstName": <string>,
          "lastName": <string>
        }

    If the embedded message type is well-known and has a custom JSON
    representation, that representation will be embedded adding a field
    `value` which holds the custom JSON in addition to the `@type`
    field. Example (for message [google.protobuf.Duration][]):

        {
          "@type": "type.googleapis.com/google.protobuf.Duration",
          "value": "1.212s"
        }
    """

    model_config = ConfigDict(
        extra='allow',
    )
    __pydantic_extra__: dict[str, Any]
    field_type: str | None = Field(None, alias='@type')
    """
    A URL/resource name that uniquely identifies the type of the serialized
    protocol buffer message. This string must contain at least
    one "/" character. The last segment of the URL's path must represent
    the fully qualified name of the type (as in
    `path/google.protobuf.Duration`). The name should be in a canonical form
    (e.g., leading "." is not accepted).

    In practice, teams usually precompile into the binary all types that they
    expect it to use in the context of Any. However, for URLs which use the
    scheme `http`, `https`, or no scheme, one can optionally set up a type
    server that maps type URLs to message definitions as follows:

    * If no scheme is provided, `https` is assumed.
    * An HTTP GET on the URL must yield a [google.protobuf.Type][]
      value in binary format, or produce an error.
    * Applications are allowed to cache lookup results based on the
      URL, or have them precompiled into a binary to avoid any
      lookup. Therefore, binary compatibility needs to be preserved
      on changes to types. (Use versioned type names to manage
      breaking changes.)

    Note: this functionality is not currently available in the official
    protobuf release, and it is not used for type URLs beginning with
    type.googleapis.com. As of May 2023, there are no widely used type server
    implementations and no plans to implement one.

    Schemes other than `http`, `https` (or the empty scheme) might be
    used with implementation specific semantics.
    """


class V1IndexData(BaseModel):
    """
    IndexData is used to index the resource for search. It's an internal
    object.
    """

    data: list[str] | None = None
    search_score: float | None = None
    """
    search_score is the score of the resource for search. Internal use only.
    """
    tenant: str | None = None
    will_be_deleted_at: AwareDatetime | None = None
    """
    Time that the resource will be deleted.
    """


class V1LicenseFileLocation(BaseModel):
    """
    LicenseFileLocation represents the location of a license, copyright, or notice in source files.
    """

    end_byte: int | None = None
    """
    The end byte of the license/copyright/notice in the file.
    """
    end_line: int | None = None
    """
    The end line of the license/copyright/notice in the file.
    """
    relative_path: str | None = None
    """
    The relative path to the file where the license/copyright/notice was found.
    """
    start_byte: int | None = None
    """
    The start byte of the license/copyright/notice in the file.
    """
    start_line: int | None = None
    """
    The start line of the license/copyright/notice in the file.
    """


class V1Meta(BaseModel):
    """
    Common fields for all Endor Labs resources.
    """

    annotations: dict[str, str] | None = None
    """
    Annotations can be used to attach metadata to a resource message.
    Annotation values can be small or large, structured or unstructured,
    and may include characters not permitted by labels.
    The keys may contain alphanumerics, underscores (_), dots (.) and dashes
    (-). The values of an annotation must be 16384 bytes or smaller.
    """
    create_time: AwareDatetime | None = None
    """
    Time the resource was created.

    Format: 2017-01-15T01:30:15.01Z
    RFC 3339: https://www.ietf.org/rfc/rfc3339.txt.
    """
    created_by: str | None = None
    """
    Name and authentication source of the user who created the object,
    for example, ewok@endor.ai@google@api-key.
    """
    description: str | None = None
    """
    Resource description. Must be less than 1024 bytes.
    """
    index_data: V1IndexData | None = None
    """
    Index data for the object.
    """
    kind: str | None = None
    """
    Resource kind, for example, HelloResponse.
    Auto-generated using the protobuf message proto.MessageName().Name().
    """
    name: str
    """
    Resource name. Must be 63 characters or less.
    """
    parent_kind: str | None = None
    """
    Parent object resource kind, for example, Project.
    """
    parent_uuid: str | None = None
    """
    Parent object UUID.
    """
    references: dict[str, GoogleprotobufAny] | None = None
    """
    Map of objects referenced in a query API.
    """
    tags: list[str] | None = None
    """
    List of tags attached to the resource.
    Tags can be used to select objects and to find collections of objects that
    satisfy certain conditions. A tag must be 255 characters or less.
    """
    update_time: AwareDatetime | None = None
    """
    Time the resource was last updated.
    Note: Updated on all create/patch/delete operations.

    Format: 2017-01-15T01:30:15.01Z
    RFC 3339: https://www.ietf.org/rfc/rfc3339.txt.
    """
    updated_by: str | None = None
    """
    Name and authentication source of the last user who updated the object,
    for example, vulnerabilityingestor@endor.ai@x509.
    """
    upsert_time: AwareDatetime | None = None
    """
    Time the resource was last upserted.

    Note:
    create_time is only set the first time the resource is created.
    upsert_time is set every time the resource is upseted.

    Format: 2017-01-15T01:30:15.01Z
    RFC 3339: https://www.ietf.org/rfc/rfc3339.txt.
    """
    version: str | None = None
    """
    Message version.
    """


class AppliedOverride(BaseModel):
    """
    All overrides that contributed to the final result, ordered root -> target.
    """

    namespace: str | None = None
    """
    Namespace where the contributing override resides.
    """
    override_uuid: str | None = None
    """
    UUID of the contributing PackageLicenseOverride in that namespace.
    """


class Copyright(BaseModel):
    """
    MergedCopyrightEntry represents a single copyright with override and selection state.
    """

    copyright_text: str | None = None
    """
    The copyright text (from override if edited, otherwise from original).
    This is the actual copyright text that will be used.
    """
    file_locations: list[FileLocation] | None = None
    """
    File locations where the copyright was found (from override if edited, otherwise from original).
    """
    identifier: str | None = None
    """
    Stable identifier/key for this copyright (file path from PackageLicense.spec.copyrights map or generated UUID for new copyrights).
    This identifier is used as the key in copyright_overrides and deselected_copyrights maps.
    For copyrights from PackageLicense: uses the file path key from PackageLicense.spec.copyrights map.
    For new copyrights added by user: uses generated UUID prefixed with "new-".
    To determine if a copyright is new: check if identifier starts with "new-" or does not exist in original PackageLicense.
    """
    is_edited: bool | None = None
    """
    Whether this copyright has been edited/overridden.
    true = copyright text has been modified from original, false = using original copyright text.
    Can be derived by checking if identifier exists in copyright_overrides map.
    """
    resolved_namespace: str | None = None
    """
    Origin of the effective value for this entry.
    """
    resolved_override_uuid: str | None = None
    selected: bool | None = None
    """
    Whether this copyright is selected for inclusion in notice file.
    true = selected (will be included), false = deselected (will be excluded).
    Defaults to true if not present in deselected_copyrights map.
    """


class License(BaseModel):
    """
    MergedLicenseEntry represents a single license with override and selection state.
    """

    file_locations: list[FileLocation] | None = None
    """
    File locations where the license was found (from override if edited, otherwise from original).
    """
    identifier: str | None = None
    """
    Stable identifier for this license (hash calculated from normalized license text).
    This identifier is used as the key in license_overrides and deselected_licenses maps.
    For licenses from PackageLicense: uses PackageLicenseInfo.hash.
    For new licenses added by user: hash is calculated from license_text using SHA256(MakeLicenseKey(normalized_text)).
    To determine if a license is new: check if identifier does not exist in original PackageLicense.
    """
    is_edited: bool | None = None
    """
    Whether this license has been edited/overridden.
    true = license text has been modified from original, false = using original license text.
    Can be derived by checking if identifier exists in license_overrides map.
    """
    license_text: str | None = None
    """
    Effective license text resolved after applying propagation across the namespace chain.
    """
    resolved_namespace: str | None = None
    """
    Origin of the effective value for this entry.
    If no override is applied, resolved_namespace will typically be "oss" and
    resolved_override_uuid will be empty.
    """
    resolved_override_uuid: str | None = None
    selected: bool | None = None
    """
    Whether this license is selected for inclusion in notice file.
    true = selected (will be included), false = deselected (will be excluded).
    Defaults to true if not present in deselected_licenses map.
    """
    source: str | None = None
    """
    Source of the original license (e.g., "code", "package_manager", "declared").
    Helps identify which category the license belongs to.
    """
    spdx_expr: str | None = None
    """
    SPDX expression for the license.
    """


class Notice(BaseModel):
    """
    MergedNoticeEntry represents a single notice with override and selection state.
    """

    file_locations: list[FileLocation] | None = None
    """
    File locations where the notice was found (from override if edited, otherwise from original).
    """
    identifier: str | None = None
    """
    Stable identifier for this notice (hash calculated from normalized notice text, or generated UUID for new notices).
    This identifier is used as the key in notice_overrides and deselected_notices maps.
    For notices from PackageLicense: uses hash or composite identifier from original.
    For new notices added by user: hash is calculated from notices_text using SHA256(MakeLicenseKey(normalized_text)).
    To determine if a notice is new: check if identifier does not exist in original PackageLicense.
    """
    is_edited: bool | None = None
    """
    Whether this notice has been edited/overridden.
    true = notice text has been modified from original, false = using original notice text.
    Can be derived by checking if identifier exists in notice_overrides map.
    """
    notices_text: str | None = None
    """
    The notice text (from override if edited, otherwise from original).
    This is the actual notice text that will be used.
    """
    resolved_namespace: str | None = None
    """
    Origin of the effective value for this entry.
    """
    resolved_override_uuid: str | None = None
    selected: bool | None = None
    """
    Whether this notice is selected for inclusion in notice file.
    true = selected (will be included), false = deselected (will be excluded).
    Defaults to true if not present in deselected_notices map.
    """


class V1PackageLicenseQuerySpec(BaseModel):
    applied_overrides: list[AppliedOverride] | None = None
    copyrights: list[Copyright] | None = None
    """
    Merged copyright entries (combines original + overrides).
    Each entry includes the copyright text, selection state, and edit status.
    """
    licenses: list[License] | None = None
    """
    Merged license entries (combines original + overrides).
    Each entry includes the license data, selection state, and edit status.
    """
    notices: list[Notice] | None = None
    """
    Merged notice entries (combines original + overrides).
    Each entry includes the notice text, selection state, and edit status.
    """
    package_license_name: str | None = None
    """
    Name of the base PackageLicense to use. If not given, then we use package_version_uuid.
    """
    package_version_uuid: str
    """
    UUID of the package version this data is for.
    """


class V1TenantMeta(BaseModel):
    """
    Tenant related data for the tenant containing the resource.
    """

    namespace: str
    """
    Namespaces are a way to organize organizational units into virtual
    groupings of resources. Namespaces must be a fully qualified name,
    for example, the child namespace of namespace "endor.prod" called "app"
    is called "endor.prod.app".
    """


class PackageLicenseQueryServiceCreatePackageLicenseQueryBody(BaseModel):
    """
    PackageLicenseQuery represents the merged result of PackageLicense + PackageLicenseOverride.
    This response combines original license data from PackageLicense with any overrides and
    selection states from PackageLicenseOverride across all namespaces in the namespace chain,
    providing a complete view for the UI.
    """

    meta: V1Meta | None = Field(None, title='metadata')
    spec: V1PackageLicenseQuerySpec | None = None
    tenant_meta: dict[str, Any] | None = Field(None, title='tenant metadata')
    uuid: str | None = None
    """
    The uuid of the resource.
    """


class V1PackageLicenseQuery(BaseModel):
    """
    PackageLicenseQuery represents the merged result of PackageLicense + PackageLicenseOverride.
    This response combines original license data from PackageLicense with any overrides and
    selection states from PackageLicenseOverride across all namespaces in the namespace chain,
    providing a complete view for the UI.
    """

    meta: V1Meta | None = Field(None, title='metadata')
    spec: V1PackageLicenseQuerySpec | None = None
    tenant_meta: V1TenantMeta | None = Field(None, title='tenant metadata')
    uuid: str | None = None
    """
    The uuid of the resource.
    """


class PackageLicenseQueryService(BaseModel):
    PackageLicenseQueryServiceCreatePackageLicenseQueryBody_1: (
        PackageLicenseQueryServiceCreatePackageLicenseQueryBody | None
    ) = Field(None, alias='PackageLicenseQueryServiceCreatePackageLicenseQueryBody')
    v1PackageLicenseQuery: V1PackageLicenseQuery | None = None