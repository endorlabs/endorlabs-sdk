{{- /* Do not modify the placement of the CommentHeader.
It is being used to identify the comments generated by Endor Labs. */ -}}
{{ .CommentHeader.Value }}

## Endor Labs Findings

{{- if and .ApiEndpoint (isNotEmptyString .ApiEndpoint.Value) }}
- [Open Endor]({{ .ApiEndpoint.Value }})
{{- end }}
- Findings in this comment: `{{ len .FindingsMap }}`

{{- range $uuid, $finding := .FindingsMap }}
### Finding `{{ $uuid }}`
- **Severity:** `{{ enumToString $finding.Spec.Level }}`
- **Endor:** [Open finding]({{- if $.ApiEndpoint -}}{{ getFindingURL $.ApiEndpoint.Value $finding }}{{- else -}}{{ getFindingURL "" $finding }}{{- end -}})
{{- if isNotEmptyString (getCustomLocation $finding) }}
- **Location:** `{{ getCustomLocation $finding }}`
{{- end }}
{{- if isNotEmptyString (getCustomCodeSnippet $finding) }}

```text
{{ fixBackticks (getCustomCodeSnippet $finding) }}
```
{{- end }}

{{- end }}

{{- if and .CommentFooter (isNotEmptyString .CommentFooter.Value) }}
{{ .CommentFooter.Value }}
{{- end }}
