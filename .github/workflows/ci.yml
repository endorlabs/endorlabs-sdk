name: CI

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  # CI Configuration - easily maintainable environment variables
  PYTHON_VERSION: "3.13"
  PYTHON_VERSIONS: '["3.12", "3.13", "3.14"]'
  PYTEST_MAXFAIL: "2"
  COVERAGE_PACKAGE: "endor_cockpit"
  UV_VERSION: "0.9.3"
  
  # Action versions - pinned for security
  ACTIONS_CHECKOUT_VERSION: "v5.0.0"
  ACTIONS_SETUP_PYTHON_VERSION: "v6.0.0"
  ACTIONS_CACHE_VERSION: "v4.3.0"
  ASTRAL_SETUP_UV_VERSION: "v3.0.0"

jobs:
  # Dependency resolution job - runs once and caches dependencies
  resolve-dependencies:
    name: Resolve Dependencies
    runs-on: ubuntu-latest
    outputs:
      cache-key: ${{ steps.cache-key.outputs.key }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v5.0.0

      - name: Set up Python
        uses: actions/setup-python@v6.0.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Generate cache key
        id: cache-key
        run: |
          echo "key=${{ runner.os }}-uv-${{ env.PYTHON_VERSION }}-${{ hashFiles('**/pyproject.toml') }}" >> $GITHUB_OUTPUT

      - name: Cache UV dependencies
        uses: actions/cache@v4.3.0
        with:
          path: ~/.cache/uv
          key: ${{ steps.cache-key.outputs.key }}
          restore-keys: |
            ${{ runner.os }}-uv-${{ env.PYTHON_VERSION }}-
            ${{ runner.os }}-uv-

      - name: Install UV
        uses: astral-sh/setup-uv@v3.0.0
        with:
          version: ${{ env.UV_VERSION }}

      - name: Install dependencies
        run: |
          uv sync --dev

  # Lint job - runs in parallel with tests
  lint:
    name: Lint
    runs-on: ubuntu-latest
    needs: resolve-dependencies
    steps:
      - name: Checkout code
        uses: actions/checkout@v5.0.0

      - name: Setup Python with UV
        uses: ./.github/actions/setup-python-uv
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache-key: ${{ needs.resolve-dependencies.outputs.cache-key }}

      - name: Run ruff linter
        run: |
          uv run ruff check .

      - name: Run ruff formatter check
        run: |
          uv run ruff format --check .

  # Test job - runs in parallel with lint
  test:
    name: Test
    runs-on: ubuntu-latest
    needs: resolve-dependencies
    strategy:
      matrix:
        python-version: ${{ fromJSON(env.PYTHON_VERSIONS) }}
      fail-fast: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v5.0.0

      - name: Setup Python with UV
        uses: ./.github/actions/setup-python-uv
        with:
          python-version: ${{ matrix.python-version }}

      - name: Run unit tests
        run: |
          uv run pytest --maxfail=${{ env.PYTEST_MAXFAIL }} --disable-warnings -m "not integration"

      - name: Run integration tests
        run: |
          uv run pytest --maxfail=${{ env.PYTEST_MAXFAIL }} --disable-warnings -m "integration"
        env:
          ENDOR_API: ${{ secrets.ENDOR_API }}
          ENDOR_API_CREDENTIALS_KEY: ${{ secrets.ENDOR_API_CREDENTIALS_KEY }}
          ENDOR_API_CREDENTIALS_SECRET: ${{ secrets.ENDOR_API_CREDENTIALS_SECRET }}
          ENDOR_NAMESPACE: ${{ secrets.ENDOR_NAMESPACE }}

  # Security scan job - runs after lint and test pass
  security:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: [resolve-dependencies, lint, test]
    if: always() && (needs.lint.result == 'success' && needs.test.result == 'success')
    steps:
      - name: Checkout code
        uses: actions/checkout@v5.0.0

      - name: Setup Python with UV
        uses: ./.github/actions/setup-python-uv
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache-key: ${{ needs.resolve-dependencies.outputs.cache-key }}

      - name: Install endorctl
        run: |
          # Install endorctl binary using official Endor Labs download
          curl -O https://api.endorlabs.com/download/latest/endorctl_linux_amd64
          chmod +x endorctl_linux_amd64
          sudo mv endorctl_linux_amd64 /usr/local/bin/endorctl
        env:
          ENDOR_API: ${{ vars.ENDOR_API }}

      - name: Verify endorctl installation
        run: |
          endorctl --version
        env:
          ENDOR_API: ${{ vars.ENDOR_API }}
          ENDOR_API_CREDENTIALS_KEY: ${{ secrets.ENDOR_API_CREDENTIALS_KEY }}
          ENDOR_API_CREDENTIALS_SECRET: ${{ secrets.ENDOR_API_CREDENTIALS_SECRET }}
          ENDOR_NAMESPACE: ${{ vars.ENDOR_NAMESPACE }}

      - name: Run endorctl host-check with diagnostics
        run: |
          echo "Running endorctl host-check with DroidGPT for diagnostics..."
          endorctl host-check --droid-gpt
        env:
          ENDOR_API: ${{ vars.ENDOR_API }}
          ENDOR_API_CREDENTIALS_KEY: ${{ secrets.ENDOR_API_CREDENTIALS_KEY }}
          ENDOR_API_CREDENTIALS_SECRET: ${{ secrets.ENDOR_API_CREDENTIALS_SECRET }}
          ENDOR_NAMESPACE: ${{ vars.ENDOR_NAMESPACE }}

      - name: Run comprehensive Endorctl security scan
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "Running PR scan with comprehensive security analysis and PR comments..."
            endorctl scan \
              --namespace ${{ vars.ENDOR_NAMESPACE }} \
              --secrets \
              --dependencies \
              --ghactions \
              --github \
              --pr \
              --enable-pr-comments \
              --scm-pr-id ${{ github.event.number }} \
              --repository-http-clone-url ${{ github.server_url }}/${{ github.repository }} \
              --output-type summary \
              --exit-on-policy-warning || echo "PR comments failed - scan completed successfully"
          else
            echo "Running branch scan with comprehensive security analysis..."
            endorctl scan \
              --namespace ${{ vars.ENDOR_NAMESPACE }} \
              --secrets \
              --dependencies \
              --ghactions \
              --github \
              --repository-http-clone-url ${{ github.server_url }}/${{ github.repository }} \
              --output-type summary \
              --exit-on-policy-warning
          fi
        env:
          ENDOR_API: ${{ vars.ENDOR_API }}
          ENDOR_API_CREDENTIALS_KEY: ${{ secrets.ENDOR_API_CREDENTIALS_KEY }}
          ENDOR_API_CREDENTIALS_SECRET: ${{ secrets.ENDOR_API_CREDENTIALS_SECRET }}
          ENDOR_NAMESPACE: ${{ vars.ENDOR_NAMESPACE }}
          GITHUB_TOKEN: ${{ secrets.ENDOR_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}
          GITHUB_API_URL: ${{ github.api_url }}

  # Coverage job - runs after tests pass
  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    needs: [resolve-dependencies, test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v5.0.0

      - name: Setup Python with UV
        uses: ./.github/actions/setup-python-uv
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache-key: ${{ needs.resolve-dependencies.outputs.cache-key }}

      - name: Run tests with coverage
        run: |
          uv run pytest --cov=${{ env.COVERAGE_PACKAGE }} --cov-report=xml --cov-report=term-missing --maxfail=${{ env.PYTEST_MAXFAIL }} --disable-warnings -m "not integration"

      - name: Display coverage summary
        run: |
          echo "Coverage report generated successfully"
          echo "Coverage data available in coverage.xml"
          echo "View detailed coverage with: uv run pytest --cov=${{ env.COVERAGE_PACKAGE }} --cov-report=html"